import { AppProducts } from './app-products';
export interface AppItems{
    // product: AppProducts;
    title: string;
    price: number;
    imageUrl:string;
    quantity: number;
    key:string;
}